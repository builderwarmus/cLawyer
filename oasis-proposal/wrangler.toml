name = "oasisproposal"
compatibility_date = "2026-07-01"

# Static-assets-only Worker (no server code) — serves index.html + images/.
# Honors _headers and .assetsignore in this directory.
[assets]
directory = "."
